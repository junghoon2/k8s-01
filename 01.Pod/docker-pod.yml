apiVersion: v1
kind: Pod
metadata:
  # annotations:
  #   diamanti.com/endpoint0: '{"network":"blue","perfTier":"high"}'
  name: docker
  labels:
    run: docker
spec:
  containers:
  - name: docker
    image: docker
    command:
      - "/bin/sh"
      - "-c"
      - "sleep inf"
